<?php

	$THUMBNAIL_OPTIONS[ 'useAbsolutePath' ] = false; ///<サムネイル画像のパスを絶対パスにする場合はtrue。

	$THUMBNAIL_OPTIONS[ 'trimming' ]        = false; ///<サムネイル画像の生成で余白が出ないようにトリミングする場合はtrue。

	$THUMBNAIL_OPTIONS[ 'filePath' ] = "file/thumbs/";

	$THUMBNAIL_OPTIONS[ 'maxWidth' ]  = 1000; ///<サムネイル化する画像の最大の幅。

	$THUMBNAIL_OPTIONS[ 'maxHeight' ] = 1000; ///<サムネイル化する画像の最大の高さ。

	$THUMBNAIL_OPTIONS[ 'jpgQuarity' ] = 75; ///<jpgサムネイルの品質(低：0～100：高)

	$THUMBNAIL_OPTIONS[ 'pngQuarity' ] = 6; ///<pngサムネイルの品質(低：9～0：高)
